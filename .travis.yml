language: dart
sudo: false
dart:
  - dev

dart_task:
  - test: --platform vm

# Only building master means that we don't run two builds for each pull request.
branches:
  only: [master]

env:
  - FLUTTER_VERSION=dev